import { OnlineofflineDirective } from './onlineoffline.directive';

describe('OnlineofflineDirective', () => {
  it('should create an instance', () => {
    const directive = new OnlineofflineDirective();
    expect(directive).toBeTruthy();
  });
});
